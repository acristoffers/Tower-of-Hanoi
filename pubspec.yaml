name: tower_of_hanoi
description: A Tower Of Hanoi game.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    version: ^0.0.0
  cupertino_icons: ^1.0.6
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
    version: ^0.0.0

flutter:
  uses-material-design: true
